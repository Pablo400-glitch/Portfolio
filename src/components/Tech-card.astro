---
import '../style/tech-card.css';
const imgBase = import.meta.env.BASE_URL;

interface Props {
    icon: string;
}

const { icon } = Astro.props;
---

<div class="tech-card">
    <div class="m-2">
        <img src={`${imgBase}${icon}`}>
    </div>
</div>
